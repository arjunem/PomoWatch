/**
 * Environment configuration for local development
 * Used when running `ng serve` or `ng build`
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5000/api'
};
